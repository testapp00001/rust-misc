[package]
name = "devops_cicd"
version = "0.1.0"
edition = "2021"
description = "Module 16: DevOps & CI/CD — GitHub Actions, releases, cross-compilation, Docker, monitoring, deployment"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
