[package]
name = "interop_wasm"
version = "0.1.0"
edition = "2021"
description = "Module 19: Interop & WASM — PyO3, WebAssembly, C embedding, plugin systems, ABI stability"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
crossbeam = "0.8"
