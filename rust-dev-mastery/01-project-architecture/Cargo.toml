[package]
name = "project_architecture"
version = "0.1.0"
edition = "2024"
description = "Module 1: Project Architecture — workspace design, crate organization, feature flags, build scripts, and release engineering"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
