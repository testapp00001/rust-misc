[package]
name = "error_handling"
version = "0.1.0"
edition = "2021"
description = "Module 2: Error Handling — error hierarchy design, thiserror, anyhow, custom errors, recovery patterns"

[dependencies]
thiserror = "2"
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
