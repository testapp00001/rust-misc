[package]
name = "serialization"
version = "0.1.0"
edition = "2021"
description = "Serde ecosystem, custom implementations, format design, schema evolution"

[dependencies]
serde = { version = "1", features = ["derive", "rc"] }
serde_json = { version = "1", features = ["raw_value"] }
