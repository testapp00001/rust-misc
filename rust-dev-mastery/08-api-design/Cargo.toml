[package]
name = "api_design"
version = "0.1.0"
edition = "2024"
description = "Module 8: API Design — public API design, trait-based APIs, versioning, documentation"

[dependencies]
serde = { version = "1", features = ["derive"] }
